import { aboutResolver } from './about.resolver'
import { configResolver } from './config.resolver'
import { usersResolver } from './users.resolver'

export default [aboutResolver, configResolver, usersResolver]
